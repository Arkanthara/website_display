name: test-project

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build dependencies
        run: npm build
      - name: test
        run: npm test
      - name: sonarqube
        run: npm test

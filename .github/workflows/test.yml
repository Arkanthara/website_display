name: test-project

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build dependencies
        run: npm install ./frontend
      - name: test
        run: npm test ./frontend
      - name: sonarqube
        run: npm test ./frontend
